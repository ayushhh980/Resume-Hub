# API Connect TODO

### Steps:
- [ ] dashboard.js: saveResume → POST /create-resume w/ form → backend
- [ ] Load resumes on login: GET /user-resumes → populate form
- [ ] Edit: select resume → PUT /update-resume/:id
- [ ] JWT authHeader() on all calls

Connected: loadResumes on load, save/update w/ ID, populateForm, JWT authHeader. Edit existing. Full CRUD/DB sync.

