# Dynamic Template TODO

### Steps:
- [ ] Update 7 templates JSON layouts
- [ ] Edit preview.js: load JSON, generate HTML/CSS for each layout (it-two-col, marketing-creative etc.), populate data, responsive
- [ ] Live preview from dashboard (sessionStorage/poll)
- [ ] Add CSS classes for layouts in style.css

Templates dynamic: preview.js generates layouts from JSON (two-column/creative etc.), populates data live, responsive/print CSS.
Dashboard preview button → templateType param → render/export.

